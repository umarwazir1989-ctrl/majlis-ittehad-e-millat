import Link from "next/link";import {getPublicActivities} from "../../lib/content/public";export const dynamic="force-dynamic";
export default async function Page(){const activities=await getPublicActivities();return <main><section className="pageHero contentHero"><div className="wrap"><span className="eyebrow">عملی پیش رفت</span><h1>سرگرمیاں اور پروگرام</h1><p>شائع شدہ سرگرمیاں، نمایاں تصاویر اور پروگرام کی تفصیلات۔</p></div></section><section className="section"><div className="wrap"><div className="activityGalleryGrid">{activities.map(a=><article className="activityGalleryCard" key={a.slug}>{a.image_url?<img src={a.image_url} alt={a.title}/>:<div className="activityImagePlaceholder">مجلس اتحادِ ملت</div>}<div><div className="articleMeta">{a.type} • {a.date}</div><h2>{a.title}</h2><p>{a.excerpt}</p><span className="locationLine">مقام: {a.location||"بعد میں شامل ہوگا"}</span><Link href={`/activities/${a.slug}`}>مکمل تفصیل ←</Link></div></article>)}</div></div></section></main>}
