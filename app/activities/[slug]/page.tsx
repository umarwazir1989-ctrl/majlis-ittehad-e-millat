import Link from "next/link";import {notFound} from "next/navigation";import {getPublicActivity} from "../../../lib/content/public";
export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=await getPublicActivity(slug);if(!a)notFound();return <main><section className="detailHero"><div className="wrap detailHead"><span className="councilBadge">{a.type}</span><h1>{a.title}</h1><div className="detailMeta">{a.date} • {a.location}</div><p>{a.excerpt}</p></div></section><section className="section"><div className="wrap readingLayout"><article className="readingCard">{a.content.map((p,i)=><p key={i}>{p}</p>)}<div className="eventPlaceholder"><span>تصاویر / میڈیا گیلری</span><strong>میڈیا ماڈیول اگلے مرحلے میں فعال ہوگا</strong></div></article><aside className="readingSide"><div className="sideBox"><h3>نوعیت</h3><strong>{a.type}</strong></div><div className="sideBox"><h3>مقام</h3><p>{a.location}</p></div><Link className="sideBack" href="/activities">← تمام سرگرمیاں</Link></aside></div></section></main>}
