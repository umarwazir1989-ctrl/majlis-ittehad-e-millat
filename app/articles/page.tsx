import Link from "next/link";import {getPublicArticles} from "../../lib/content/public";export const dynamic="force-dynamic";
export default async function Page(){const articles=await getPublicArticles();return <main><section className="pageHero contentHero"><div className="wrap"><span className="eyebrow">فکر • تحقیق • مکالمہ</span><h1>مضامین و افکار</h1><p>Admin سے شائع ہونے والے مضامین خودکار طور پر یہاں ظاہر ہوں گے۔</p></div></section><section className="section"><div className="wrap"><div className="articleGrid imageArticleGrid">{articles.map((a,i)=><article className="articleCard imageArticleCard" key={a.slug}>{a.image_url?<div className="articleImage"><img src={a.image_url} alt={a.title}/><span>{a.category}</span></div>:<div className="articleVisual"><span>{a.category}</span><b>{String(i+1).padStart(2,"0")}</b></div>}<div className="articleBody"><div className="articleMeta">{a.author} • {a.date}</div><h2>{a.title}</h2><p>{a.excerpt}</p><Link href={`/articles/${a.slug}`}>مکمل مضمون پڑھیں ←</Link></div></article>)}</div></div></section></main>}
