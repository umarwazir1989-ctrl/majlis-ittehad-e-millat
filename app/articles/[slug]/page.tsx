import Link from "next/link";import {notFound} from "next/navigation";import {articles,getArticle} from "../../../data/articles";
export function generateStaticParams(){return articles.map(x=>({slug:x.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=getArticle(slug);if(!a)notFound();return <main><section className="detailHero"><div className="wrap detailHead"><span className="councilBadge">{a.category}</span><h1>{a.title}</h1><div className="detailMeta">{a.author} • {a.date}</div><p>{a.excerpt}</p></div></section><section className="section"><div className="wrap readingLayout"><article className="readingCard">{a.content.map((p,i)=><p key={i}>{p}</p>)}<div className="sourceNote">یہ ابھی ڈمی مضمون ہے؛ حتمی مواد بعد میں replace ہوگا۔</div></article><aside className="readingSide"><div className="sideBox"><h3>زمرہ</h3><strong>{a.category}</strong></div><div className="sideBox"><h3>مصنف</h3><p>{a.author}</p></div><Link className="sideBack" href="/articles">← تمام مضامین</Link></aside></div></section></main>}
