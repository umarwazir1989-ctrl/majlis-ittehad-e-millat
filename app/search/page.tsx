import SearchHub from "../../components/SearchHub";import {getPublicActivities,getPublicArticles,getPublicPeople} from "../../lib/content/public";export const dynamic="force-dynamic";
export default async function Page(){const [articles,activities,people]=await Promise.all([getPublicArticles(),getPublicActivities(),getPublicPeople()]);return <main><section className="pageHero searchPageHero"><div className="wrap"><span className="eyebrow">جامع تلاش</span><h1>ویب سائٹ میں تلاش کریں</h1><p>Database اور موجودہ مواد میں مضامین، سرگرمیاں اور شخصیات تلاش کریں۔</p></div></section><section className="section"><div className="wrap"><SearchHub articles={articles} activities={activities} people={people}/></div></section></main>}
