import Link from "next/link";import {getPublicPeople} from "../../lib/content/public";export const dynamic="force-dynamic";
export default async function Page(){const all=await getPublicPeople();const people=all.filter(x=>x.council==="مجلس بزرگان");return <main><section className="pageHero peopleHero"><div className="wrap"><span className="eyebrow">علمی رہنمائی</span><h1>مجلس بزرگان</h1><p>علمی وقار، تجربے اور اجتماعی بصیرت کی نمائندہ سطح۔</p></div></section><section className="section"><div className="wrap"><div className="peopleGrid">{people.map(p=><article className="profileCard" key={p.slug}><div className="profileTop"><div className="profileAvatar">{p.initials}</div><span className="councilBadge">{p.council}</span></div><h3>{p.name}</h3><p className="designation">{p.designation}</p><p>{p.summary}</p><Link className="profileLink" href={`/people/${p.slug}`}>مکمل پروفائل دیکھیں ←</Link></article>)}</div></div></section></main>}
