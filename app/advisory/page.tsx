import Link from "next/link";import {getPublicPeople} from "../../lib/content/public";export const dynamic="force-dynamic";
export default async function Page(){const people=(await getPublicPeople()).filter(x=>x.council==="مجلس مشاورت");return <main><section className="pageHero peopleHero advisoryHero"><div className="wrap"><span className="eyebrow">اجتماعی مشاورت</span><h1>مجلس مشاورت</h1><p>علمی، فکری اور ملی امور میں مشترکہ مشاورتی حلقہ۔</p></div></section><section className="section"><div className="wrap"><div className="peopleGrid">{people.map(p=><article className="profileCard advisoryCard" key={p.slug}><div className="profileTop">{p.image_url?<img className="profilePhoto" src={p.image_url} alt={p.name}/>:<div className="profileAvatar">{p.initials}</div>}<span className="councilBadge">{p.council}</span></div><h3>{p.name}</h3><p className="designation">{p.designation}</p><p>{p.summary}</p><Link className="profileLink" href={`/people/${p.slug}`}>مکمل پروفائل دیکھیں ←</Link></article>)}</div></div></section></main>}
