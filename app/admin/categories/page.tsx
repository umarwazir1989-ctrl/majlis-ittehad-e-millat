import Link from "next/link";import {articles} from "../../../data/articles";import {activities} from "../../../data/activities";
export default function Page(){const cats=Array.from(new Set(articles.map(x=>x.category)));const types=Array.from(new Set(activities.map(x=>x.type)));return <main className="adminPage"><section className="adminSubHero"><div className="wrap"><Link href="/admin">ڈیش بورڈ ←</Link><h1>زمرے اور اقسام</h1></div></section><section className="section"><div className="wrap"><div className="g2"><div className="adminPanel"><h2>مضامین کے زمرے</h2>{cats.map(x=><div className="categoryAdminItem" key={x}>{x}</div>)}</div><div className="adminPanel"><h2>سرگرمیوں کی اقسام</h2>{types.map(x=><div className="categoryAdminItem" key={x}>{x}</div>)}</div></div><div className="adminAlert" style={{marginTop:"24px"}}>زمرے add/edit/delete کرنے کی حقیقی سہولت database connection کے بعد فعال ہوگی۔</div></div></section></main>}
